CREATE TABLE reassurance (
  id        CHAVE                  NOT NULL,
  id_shop   CHAVE                  NOT NULL,
  file_name CHARACTER VARYING(100) NOT NULL,
  CONSTRAINT pk_reassurance PRIMARY KEY (id)
);